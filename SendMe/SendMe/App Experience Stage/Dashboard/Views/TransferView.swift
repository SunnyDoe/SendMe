import SwiftUI

struct TransferView: View {
    var body: some View {
        VStack {
            Text("Transfer")
                .font(.largeTitle)
                .fontWeight(.bold)
            Text("Coming soon...")
                .foregroundColor(.secondary)
        }
    }
} 